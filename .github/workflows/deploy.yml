
# .github/workflows/deploy.yml

name: Deploy Astro to GitHub Pages

on:
  push:
    branches: ["main"] # Action runs every time you push to the 'main' branch
  workflow_dispatch:

jobs:
  build:
    name: Build and Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm' # Cache node modules for faster builds
          
      - name: Install dependencies
        run: npm install
        
      - name: Build Astro site (npm run build creates the 'dist' folder)
        run: npm run build
        
      # Deployment action: pushes the static 'dist' content to the gh-pages branch
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist # The folder containing the final static website files
          publish_branch: gh-pages # The special branch GitHub Pages serves from
